<div class="container">
  <h1>つくれぽ</h1>
  <%= form_for @review do |f| %>
    レポート
    <div class="">
    <%= f.text_area :content, class: 'materialize-textarea' %>
    </div>
    画像
    <%= f.file_field :image %>
    <%= f.submit 'つくれぽを送る' %>
  <% end %>  
</div>
